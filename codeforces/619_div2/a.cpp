#include <bits/stdc++.h>

using namespace std;

int main(){
    int t;
    while(t--){
        string a, b, c;
        cin >> a >> b >> c;

        for (int i=0; i<c.length(); i++){
            if ((c[i]==[a[i]) && (c[i]==a[i])) continue;
        }
    }

}
